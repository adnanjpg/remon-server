# Production environment configuration
# Overrides default.toml settings. Sensitive values (JWT secret, notification
# tokens) should be set via environment variables, not committed here.

[logging]
level = "info"
format = "json"
