meta {
  name: Get Docker Status
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/docker/status
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}

docs {
  Check if Docker is available on the system.
  Returns availability status and Docker version if available.
}
